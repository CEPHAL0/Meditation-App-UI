import 'package:flutter/material.dart';

class AllExercisesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('All Exercises'),
      ),
      body: Center(
        child: Text('All Exercises Screen'),
      ),
    );
  }
}
